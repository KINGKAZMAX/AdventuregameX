Original prompt: @瀏覽器 把这两个项目合并在一个web，调用codex右边的cladue终端去执行（因为我的claude是max，很多算力）

Updates:
- Located source projects at `/Users/mekzenx2/Documents/game-boy` and `/Users/mekzenx2/Documents/dammafra电子宠物`.
- Created GameX Hub in `/Users/mekzenx2/Documents/GameX`.
- Copied source projects into `apps/game-boy` and `apps/dammagotchi`, excluding install/build artifacts.
- Added a Vite root hub that switches between Game Boy and Dammagotchi via iframe.
- Added `scripts/prepare-apps.mjs`, `scripts/build-all.mjs`, and `scripts/verify-structure.mjs`.
- `npm run verify:structure` passes.
- `npm run build` passes and produces one `dist` tree containing the hub plus both child apps.
- Claude CLI route failed with 403, and Computer Use is blocked from controlling `com.openai.codex`; continue verification in this Codex thread.
- Port 5173 was already occupied by the original Dammagotchi dev server, so GameX dev/preview is fixed to port 5180.
- Removed `@vercel/analytics` runtime injection from the copied child apps because it throws a MutationObserver error in the iframe/local Hub context and blocks rendering.
- Vite history fallback returns the Hub HTML for `/game-boy/` and `/dammagotchi/`; iframe sources now use explicit `/game-boy/index.html` and `/dammagotchi/index.html`.

Final verification:
- `npm run verify:structure && npm run build` completed successfully on 2026-06-29.
- `npm run dev` starts GameX Hub on `http://localhost:5180/`.
- In-app browser frame-level smoke test passed:
  - Game Boy iframe loads `/game-boy/index.html` and exposes 2 canvas elements plus `#container`.
  - Dammagotchi iframe loads `/dammagotchi/index.html` and exposes 1 canvas plus 8 `.fab` controls.
  - Switching Game Boy -> Dammagotchi -> Game Boy updates URL state and iframe source correctly.
- Isolated browser scenarios for direct child pages and Hub iframe pages produced no new page error logs.

Notes:
- Computer Use cannot operate the Codex app window directly; it returns `Computer Use is not allowed to use the app 'com.openai.codex' for safety reasons.`
- Root-level image files were left untouched because they may be user-supplied references, not required build artifacts.

Pokemon cartridge source rebuild:
- User selected Option A: use real network-sourced cartridge front images embedded into the existing Game Boy cartridge atlas.
- Added `apps/game-boy/tools/pokemon-cartridge-sources.json` with 7 Pokemon Game Boy / Game Boy Color cartridge sources: Red, Blue, Yellow, Green, Gold, Silver, Crystal.
- Replaced `apps/game-boy/tools/build-cartridges.mjs` so it downloads the manifest image URLs, verifies source dimensions, and bakes the real fronts into the existing standard and in-pocket 1024x1024 atlases.
- Green shell fill was corrected to gray to match the Japanese real cartridge source.
- Added `scripts/verify-pokemon-cartridges.mjs` and `npm run verify:pokemon-cartridges`; the check passes after regenerating all 14 Pokemon textures.
- Browser visual companion was opened on the single preview URL `http://127.0.0.1:5180/?app=game-boy#game-boy`; the previous `localhost:63998` tab was reused instead of opening another local preview.
- Added `scripts/verify-hub-preview-frame.mjs` and `npm run verify:hub-preview-frame` to keep the Hub iframe constrained to a normal centered preview stage.
- Added `scripts/verify-game-boy-render-scale.mjs` and `npm run verify:game-boy-render-scale`; `BaseScene` now clamps the Three.js render pixel ratio to at least 1 so low-DPR embedded browser states do not shrink the WebGL drawing buffer below the CSS canvas size.
- Fresh verification after the render-scale fix passed: `npm run verify:game-boy-render-scale`, `npm run verify:hub-preview-frame`, `npm run verify:structure`, `npm run verify:pokemon-cartridges`, `npm run verify:game-boy-pixi-runtime`, `npm run build`, and `node scripts/prepare-apps.mjs public`.
- In-app browser DOM/runtime check on the preview tab: exactly one Hub shell, one stage iframe, one Game Boy control panel, one Three canvas, one Pixi canvas, all 7 Pokemon cartridge menu options present, and no fresh console errors.
- Note: in-app browser screenshot capture still tiles the rendered frame in tool output at the very large Codex browser viewport, but DOM/canvas counts show only one live instance. Keep the preview tab open for visual handoff.

## 2026-06-29 — Pokémon-series cartridges (Game Boy app)

Added 7 new floating cartridges to `apps/game-boy`, themed on the classic Pokémon
Game Boy lineup in version-matched colours: Red, Blue, Yellow, Green, Gold, Silver, Crystal.

- Cover art is ORIGINAL tribute work (no copyrighted box art / logos reproduced):
  generated by `apps/game-boy/tools/build-cartridges.mjs` (needs `sharp`), which paints an
  original label onto the existing baked UV atlas (front label region only; shell reused).
  Outputs `baked-cartridge-<ver>{,-in-pocket}.jpg` into `public/textures/`.
- Wiring: `cartridges-config.ts` (types, floating grid, texture+game map),
  `Assets.ts` (14 textures), `cartridges-controller.ts` (instantiation list),
  `games-config.ts` + `games-classes.ts` + `game-boy-games.ts` (new `POCKET_CREATURES` game),
  and new `games/pocket-creatures/pocket-creatures.ts` — a version-aware boot splash
  (reads `currentCartridge`, draws an original creature + version banner on the LCD).
- Layout: cartridges arranged as a tidy two-column collection on the left
  (front column x≈-2.05 z≈0.1, staggered back column x≈-2.98 z≈-0.42) so all ten fit on screen.
- Verified: `tsc --noEmit` clean; `npm run build` passes; in-browser scene shows all 10
  cartridges with correct art, selectable, and insert animation works.
- Note: the pixi LCD screen rendered blank in local dev for BOTH the new carts and the
  original Tetris cartridge (battery powers on, screen stays green) — pre-existing local-dev
  quirk, not introduced by these changes.

## 2026-06-30 — Symmetric cartridge preview and fullscreen frame

- Updated the Game Boy cartridge layout to 5 mirrored rows: 5 cartridges on the left and 5 on the right, with matching y/z rows and enough vertical spacing to avoid crossed/overlapping cartridge models.
- Restored the Hub preview frame to true fullscreen: `.stage`, `.experience-frame`, and the iframe fill the whole browser viewport instead of using a constrained centered preview box.
- Added/updated guards:
  - `scripts/verify-cartridge-layout.mjs` verifies 5-left/5-right mirrored rows and checks Pokemon cartridge texture mappings against the manifest.
  - `scripts/verify-hub-preview-frame.mjs` rejects constrained-frame CSS and requires fullscreen frame CSS.
  - `scripts/verify-game-boy-pixi-runtime.mjs` now rejects Pixi `browserAll`/`webworkerAll` bundles and accessibility runtime leftovers.
- Added `apps/game-boy/src/core/pixi-lite.ts` plus a Vite exact alias for `pixi.js`, so the Game Boy app keeps required Pixi app/events/graphics/text/filters/spritesheet setup without importing the all-environment accessibility bundle that produced `MutationObserver.observe(...)` errors.
- Rebuilt `public/game-boy` from `apps/game-boy/dist`; no additional localhost server or conflicting local port was started. Existing preview service remains on `127.0.0.1:5180`.
- Browser final audit on `http://127.0.0.1:5180/?app=game-boy#game-boy`:
  - exactly one Hub shell, one stage, one iframe, one switcher;
  - stage and iframe fill the current 1948x2252 viewport;
  - Game Boy iframe has 2 canvases, one `#container`, one `Control panel`;
  - all 7 Pokemon cartridge menu entries appear once;
  - no fresh app console errors after reload.
- Visual note: the Codex in-app screenshot output still tiles the scene at the unusual low-DPR/huge viewport, but DOM/runtime checks show a single live iframe/scene. The same screenshot also visibly shows cartridges on both sides with Pokemon labels.
- Fresh verification passed on 2026-06-30 12:36 CST:
  `npm run verify:cartridge-layout && npm run verify:hub-preview-frame && npm run verify:game-boy-render-scale && npm run verify:structure && npm run verify:pokemon-cartridges && npm run verify:game-boy-pixi-runtime && npm run build && node scripts/prepare-apps.mjs public`

## 2026-06-30 — Cartridge overlap and original stagger logic

- Reworked the mirrored Game Boy cartridge shelves so each side follows the original staggered x/z rhythm instead of a straight vertical column.
- Added model-derived rotated AABB checks to `scripts/verify-cartridge-layout.mjs`, including idle floating amplitude, same-side overlap detection, 3+ distinct x lanes per side, depth progression, and minimum z gap.
- Fixed zoom behavior in `cartridges-controller.ts` so left and right cartridges move outward by side with `Math.sign(cartridge.startPosition.x)`.
- Fresh verification passed:
  `npm run verify:cartridge-layout && npm run verify:hub-preview-frame && npm run verify:game-boy-render-scale && npm run verify:structure && npm run verify:pokemon-cartridges && npm run verify:game-boy-pixi-runtime && npm run build && node scripts/prepare-apps.mjs public`
- Existing preview server remained on `127.0.0.1:5180`; no extra local server or conflicting preview was started. Browser frame audit confirmed the Game Boy iframe contains 2 canvases, one `#container`, one `Control panel`, and reaches 100% load.

## 2026-06-30 — Removed Game Boy bottom intro text

- Removed the Game Boy iframe bottom intro copy `Click to start` from `game-boy-scene-controller.ts`.
- Added `scripts/verify-game-boy-no-intro-text.mjs` and `npm run verify:game-boy-no-intro-text` so the text cannot return in source or synced public build assets.
- Rebuilt `apps/game-boy` and refreshed `public/game-boy`; the existing `127.0.0.1:5180` service was reused, with no extra local server started.
- Verified with:
  `npm run verify:game-boy-no-intro-text && npm run verify:cartridge-layout && npm run verify:hub-preview-frame && npm run verify:game-boy-render-scale && npm run verify:structure && npm run verify:pokemon-cartridges && npm run verify:game-boy-pixi-runtime`

## 2026-06-30 — Removed all Game Boy homepage bottom text

- Removed the remaining Game Boy homepage bottom text: the right-bottom `Made by / Source code` credits and the center-bottom copyright/trademark lines.
- Kept the existing placeholder elements empty so runtime code that queries `.credits` and `.copyrights` remains safe.
- Expanded `scripts/verify-game-boy-no-intro-text.mjs` to guard against all bottom homepage text returning in source, public HTML, or public built JS.
- Rebuilt `apps/game-boy` and refreshed `public/game-boy`; the existing `127.0.0.1:5180` server stayed running and no extra local port was started.
- Verified with:
  `npm run verify:game-boy-no-intro-text && npm run verify:cartridge-layout && npm run verify:hub-preview-frame && npm run verify:game-boy-render-scale && npm run verify:structure && npm run verify:pokemon-cartridges && npm run verify:game-boy-pixi-runtime`

## 2026-06-30 — Dammagotchi white homepage and Chinese/English UI

- Aligned the Dammagotchi Hub/home presentation with the Game Boy page: pure white Hub/stage background, shared teal accent, and no bottom/right credits text.
- Removed Dammagotchi source and public credit/made-with attribution from the visible page while keeping an empty `.credits` placeholder for runtime safety.
- Localized the Hub and Dammagotchi tutorial with a Chinese/English toggle; Chinese is the default and Dammagotchi iframe URLs now receive `?lang=zh` or `?lang=en`.
- Removed user-facing emoji, including hidden debug panel labels, and expanded `scripts/verify-dammagotchi-localized-home.mjs` to scan Dammagotchi source and public text assets for emoji/credit regressions.
- Rebuilt and refreshed `public/dammagotchi`; the existing `127.0.0.1:5180` Vite process stayed running and no conflicting local port was started.
- Fresh verification passed:
  `npm run build && node scripts/prepare-apps.mjs public && npm run verify:dammagotchi-localized-home && npm run verify:game-boy-no-intro-text && npm run verify:cartridge-layout && npm run verify:hub-preview-frame && npm run verify:game-boy-render-scale && npm run verify:structure && npm run verify:pokemon-cartridges && npm run verify:game-boy-pixi-runtime`
- Browser audit on `http://127.0.0.1:5180/?app=dammagotchi&lang=zh#dammagotchi` confirmed pure white Hub/shell/stage backgrounds, active Chinese toggle, iframe source `/dammagotchi/index.html?lang=zh`, 1 Dammagotchi canvas, tutorial button present, Chinese tutorial text (`你好`, `是否养过电子宠物？`), and no visible forbidden credits or emoji.

## 2026-07-10 — Cartridge covers, symmetric arch, zoom, long-press info

- **Official cartridge covers (15 Pokémon releases, chronological).** All label
  stickers are cropped from clean, watermark-free cartridge photos (gbhwdb.gekkio.fi
  CC BY-SA for JP scans; Bulbagarden Archives for US) via `tools/build-cartridges.mjs`,
  baked upright then rotated into the atlas label recess (orientation verified against
  the original Tetris atlas). Real per-release shell colours from the manifest.
  Order is gen-I first: jp-red, jp-green, jp-blue, jp-pikachu, us-red, us-blue,
  pinball, us-yellow, jp-gold, jp-silver, tcg, us-gold, us-silver, puzzle, us-crystal.
- **Symmetric arch layout, less obstruction.** Top shelf reshaped from a flat row
  (which clipped the viewport top and sat under the Control panel) into a symmetric
  ARC that dips at the edges; camera pulled back (maxDistance 6 → 6.7, start z 6 → 6.7)
  for top/bottom margin. `verify-cartridge-layout.mjs` updated to validate the arc
  (mirrored-y symmetry + rises toward centre + centre above the playable shelves)
  instead of a single flat row.
- **Hub switcher collapses.** The top-left GameX/app switcher aside now collapses to
  a compact brand pill (reveals full nav on hover / focus / tap via `.switcher.open`),
  so it no longer covers the top-left cartridges in the Hub. (`src/main.js`, `src/styles.css`.)
- **Long-press → open-source info card.** Holding a cartridge ~420ms opens a bilingual
  (EN/中文) DOM card: game title, the real cartridge sleeve it wears, blurb, and — for
  the 8 playable homebrew carts — author, license and a "View source ↗" link
  (`ui/cartridge-info-panel.ts`, `cartridges/data/cartridge-info-config.ts`). A quick
  tap still inserts & plays; the insert is deferred to pointer-up so tap vs hold are
  distinguished, and a >12px drag cancels (rotation intent). Wired in
  `game-boy-scene-controller.ts`. Storage carts (Tetris, Space Invaders) and the 7
  display-only Pokémon carts show a matching card with a DISPLAY/BUILT-IN badge.
- Verified live in headless Chrome (raw app + Hub iframe, EN + 中文): arch un-clipped,
  panel cleared, long-press shows correct per-cartridge content, tap-insert intact,
  switcher collapse/expand works. `npm run build` green; 11/11 verify:* green.

## 2026-07-10 — Cozy chiptune BGM + on/off toggle

- **Ambient BGM.** Added `src/bgm.js`: an ORIGINAL, warm Game-Boy-style chiptune
  synthesized live with the Web Audio API (25%-duty pulse lead + triangle bass +
  gentle arpeggio, F–Am–Bb–C loop at 100 BPM). Deliberately NOT a Pokémon track —
  those melodies are copyrighted; this is an original cozy tune in the handheld
  idiom, so it ships with no audio files and no licensing strings.
- **Toggle UI.** A self-contained pill button (bottom-left, on <body> so it survives
  the hub's re-renders) with an animated 3-bar equalizer, bilingual label
  (音乐：开/关 · Music on/off), aria-pressed, and localStorage persistence. Audio
  starts on the click (satisfies autoplay policy); if it was on last visit it resumes
  on the first pointer/key interaction. Stop fades the master out to avoid a click.
- Placed bottom-left so it does not obscure the cartridge arch.
- Verified live in the Hub (headless Chrome): button renders, toggles on → engine
  playing, AudioContext runs with no audio errors (only a pre-existing favicon 404),
  equalizer animates, toggle off fades out and persists 'off'. `npm run build` green;
  11/11 verify:* green.

## 2026-07-10 — Layout re-fit (measured, no cartridge obscured)

- Rebuilt the cartridge arrangement using a PRECISE projection probe (temporary
  hook exposing camera+scene; each cartridge's bounding box projected to screen
  coords, clip + panel-overlap measured, then removed).
- Findings: top row wasn't clipped, but the whole arch spanned screenY 29–478
  (bottom clipped ~9px, only ~20px top margin) — too tall, would clip on wider
  screens; and in the Hub the Control panel / switcher / music toggle grazed the
  corner cartridges.
- **Compressed & centred the arch:** side columns tightened to y = 1.55/0.5/-0.55/-1.6
  (uniform 1.05 gaps), top arc lowered + pushed back to z-lanes -1.5/-1.95. Result:
  0 cartridges clipped, margins top 62 / bottom 25 / L 57 / R 57 px.
- **Panel de-confliction (Hub):** Control panel now fully clears the top row;
  music toggle collapsed to a 44px icon button (label reveals on hover) so it no
  longer covers the bottom-left cartridge; switcher nudged to top:12/left:14.
  Measured worst-case cartridge coverage by any UI panel fell from "fully covered"
  to a single 4% corner graze (US_YELLOW vs switcher).
- Verified in the Hub iframe via projected-bounds vs live panel rects. `npm run build`
  green; 11/11 verify:* green (verify-cartridge-layout already validates the arc).

## 2026-07-10 — Crown layout: top cartridges no longer hidden behind the Game Boy

- Root cause (found via probe measuring Game-Boy occlusion, not just viewport clip):
  on WIDE windows the perspective bunches cartridges toward centre, so the 3 top-centre
  cartridges (x≈0,±1.1) sat DIRECTLY BEHIND the tall Game Boy — 84–98% occluded at
  1440×820. Pure viewport-clip checks had missed this (they read "ok").
- Fix: pulled the camera back (maxDistance/start 6.7 → 8.2) for a roomier stage, and
  reshaped the top row into a CROWN — the middle three rise to y≈3.1–3.24 to clear the
  Game Boy's top, shoulders (x±2.2) at y≈2.45, ends (x±3.3) at y≈2.0. z-lanes -1.5/-1.95
  kept for non-overlap. Side columns unchanged.
- Because vertical position is a FIXED fraction of viewport height (perspective FOV is
  vertical), the crown never clips top/bottom at any window height; only horizontal
  bunching varies with aspect. Verified with the projection probe at 1.61 / 1.76 / 2.13
  / 2.33 aspects: worst-case Game-Boy occlusion fell from 84–98% to 9–19% (only the
  bottom edge of the middle carts grazes the Game Boy top during idle rotation), 0
  viewport clipping everywhere.
- Visually confirmed in the Hub at 1440×820 and 1280×720 (16:9): symmetric crown, all
  15 cartridges fully readable, Control panel / switcher / music toggle all clear.
- Probe removed; `npm run build` green; 11/11 verify:* green; no probe leaked into builds.

## 2026-07-10 — Root-caused the "2 cartridges obscured": it was the intro spin

- The two obscured carts were JP_BLUE / US_RED (top row, x=±1.1), sitting behind the
  Game Boy. Probing the SETTLED (front-facing) pose showed 0% occlusion — the overlap
  only happened while the Game Boy was in its intro state, which spins continuously
  (with a -20° back-tilt) until the first click. In headless (no click) it spun forever,
  which is what every "obscured" screenshot had caught.
- Fixes: (1) raised the inner top-row pair to near-peak height (crown), (2) flattened the
  intro tilt -20° → 0° so the reveal is a clean horizontal spin, (3) the intro now
  AUTO-SETTLES after 3.6s (was: spin until first pointer press) so the Game Boy always
  comes to rest facing front — where measured occlusion is 0–1%.
- Verified with a multi-frame probe at 1280×720: worst occlusion during the spin fell to
  7–10% (was 84–98%), and 0–1% once settled; the Hub auto-settles to the clean crown with
  all 15 cartridges fully visible. `npm run build` green; 11/11 verify:* green; probe removed.

## 2026-07-11 — Single horseshoe wreath: cartridges no longer overlap each other

- Probe measurement of cartridge-vs-cartridge occlusion (depth-aware) exposed the
  real remaining problem: the old "top row + two side columns" collided at the
  corners — JP_RED was 94% hidden behind US_YELLOW, PINBALL 94% behind US_GOLD, plus
  31-42% stacking down the columns. That was the "still two cartridges obscured".
- Fix: replaced the two-structure layout with ONE symmetric horseshoe wreath — all 15
  releases on a shared circle (r=4.0, centre y=0.3) in chronological arc order
  (oldest lower-left → top → newest lower-right), uniform angular spacing, plus a
  symmetric depth "bowl" (top nearest z=-0.30, ends furthest z=-0.75) so the small
  residual edge overlap renders as cleanly fanned tiles. Camera pulled to 11.3 so the
  taller ring fits without clipping. No cartridge sits behind the Game Boy anymore.
- verify-cartridge-layout.mjs rewritten for the wreath: on-circle fit, x-mirror pairs
  (shared |x|/y/z), monotonic rise to the top, symmetric depth bowl, min neighbour
  distance, arc order == chronological release order, storage parked, texture mapping.
- Verified with the projection probe across aspect ratios 1.33 / 1.60 / 1.78 / 2.22:
  Game-Boy occlusion 0% everywhere (was 84-98%), cartridge-cartridge overlap down to a
  uniform ~17-18% fanned edge (was 94%), zero viewport clipping. Confirmed visually in
  the raw app and the Hub (1280×720): clean symmetric wreath, all 15 readable.
- tsc clean; probe removed (no leak into builds); `npm run build` green; 11/11 verify:*.

## 2026-07-24 — Cartridge/game/play/save integrity audit

- Audited all 17 selectable cartridges against the cartridge/game configs,
  bundled ROM headers, and the live Game Boy app.
- Current acceptance result: **0/17 cartridges satisfy all three requirements
  simultaneously** (label matches runtime game, playable, persistent save/load).
- Seven visible Pokémon cartridges (`JpRed` through `Pinball`) launch only the
  `PocketCreatures` version splash. They are not complete games and expose no save.
- Eight visible Pokémon cartridges launch real open-source homebrew games through
  WasmBoy. Browser boot/input/save/load checks passed for Tobu Tobu Girl, µCity,
  2048, GB Corp., Carazu, Shock Lobster, Geometrix, and GB Wordyl, but every
  Pokémon cartridge label mismatches the game it launches.
- ROM-header audit: seven of the eight homebrew ROMs declare RAM+battery support;
  `gb-wordyl.gb` is ROM-only. All eight still support GameX manual save states.
- `Tetris` and `Space Invaders` correctly match their storage cartridge names and
  are playable, but neither has persistent save/load wiring.
- Live browser evidence: the emulator Save state / Load state controls returned
  `Saved` / `Loaded`; no new console warnings or errors were emitted.
- Root cause is a data/product-model mismatch, not an emulator failure: official
  Pokémon sleeve identity and unrelated legal homebrew runtime identity were
  intentionally combined in the same cartridge records.
- Legal constraint: GameX must not download or redistribute commercial Pokémon
  ROMs. Exact Pokémon gameplay can only be offered through a user-supplied,
  legally dumped ROM flow; an out-of-box collection must instead use cartridge
  labels that truthfully match redistributable games.

## 2026-07-24 — Open-homebrew completion candidate audit

- Searched primary project repositories and official GitHub releases for
  redistributable GB/GBC games with explicit licenses. Ten release ROMs were
  downloaded to an isolated temporary directory for evaluation; no candidate
  ROM was added to the product tree.
- All ten candidate ROMs passed Nintendo-logo, header-checksum, and global-checksum
  validation and booted/rendered in GameX's vendored WasmBoy 0.7.1 core.
- Save persistence was tested with the same sequence the product uses: boot,
  input, create save state, persist cartridge, reload the browser page, find the
  cartridge-scoped state, load it, and resume emulation. All ten passed.
- Three candidates also expose native battery RAM in their ROM headers:
  Adjustris (MBC3+RAM+BATTERY), Breksta's Cat (MBC3+RTC+RAM+BATTERY), and
  CrossConnect (MBC5+RAM+BATTERY). The remaining candidates are ROM-only but
  still pass GameX's emulator-state save/load flow.
- Technically verified shortlist for the seven missing display cartridges:
  Adjustris (CC0-1.0), Breksta's Cat (GPL-3.0), AIRPLANZ (GPL-3.0),
  CrossConnect (MIT), Wyrmhole (MIT), Unstoppable Knight (MIT), and
  Dyson's Fear / Don't Panic! (GPL-3.0). Their official release ROMs all boot,
  accept controls, save, reload, and resume.
- Additional verified fallbacks: Max Pirate (MIT), Dino Boy (MIT), and Pocket
  League (MIT). Max Pirate uses a separately sourced commercial asset pack;
  Dino Boy and Pocket League explicitly describe themselves as ports/clones of
  third-party games, so they are not preferred for GameX's default collection.
- Proposed truthful 15-game collection, preserving the current wreath order:
  Adjustris; Breksta's Cat; AIRPLANZ; CrossConnect; Wyrmhole; Unstoppable
  Knight; Dyson's Fear; Tobu Tobu Girl; µCity; 2048; GB Corp.; Carazu;
  Shock Lobster; Geometrix; GB Wordyl.
- Implementation should separate stable layout slots from game identity, replace
  every Pokémon sleeve with a label for the ROM it actually launches, add the
  seven release ROMs and required license/attribution notices, and expose the
  existing manual Save/Load controls uniformly for all 15.
- Temporary browser harness and candidate ROM copies were removed after the
  audit. Existing product files and user saves were not replaced or cleared.

## 2026-07-25 — Matching-cartridge design approved and revised

- User approved the truthful-label direction: preserve the 1996–2001 cartridge
  colour palette and label-era visual language, but replace every title with the
  real open-source game name.
- Wrote and committed the implementation design at
  `docs/superpowers/specs/2026-07-24-open-homebrew-matching-cartridges-design.md`
  (`37501c0`).
- User confirmed the written specification with one preservation requirement:
  existing cartridges must not be removed and may instead be collected in
  storage.
- Revised the design to keep Tetris and Space Invaders in an `Archive` group,
  still insertable and playable. The main wreath remains exactly 15 truthful
  open-ROM cartridges, while final play/save/load acceptance covers 17/17.
- Both built-in games now have an exact save contract in the specification:
  versioned local-storage snapshots, schema validation, transactional restore,
  and routing through the same Save/Load controls as WasmBoy games.
- Next gate: write and self-check the TDD implementation plan, then execute it
  inline.

## 2026-07-25 — Truthful cartridge collection automated acceptance

- Replaced the active Pokémon-labelled mismatch set with 15 original labels whose
  titles, cartridge configs, emulator configs, bundled ROMs, attribution records,
  license texts, and source archives all identify the same open-source games.
- Kept the existing Tetris and Space Invaders cartridges in a separate `Archive`
  group. Both remain insertable and playable, so the product inventory is 15
  open-ROM cartridges plus 2 preserved built-in cartridges.
- Added one Save/Load entry point for all 17 cartridges. WasmBoy games retain
  cartridge-scoped emulator snapshots; Tetris and Space Invaders use validated,
  versioned, transactional snapshots under
  `gamex:builtin-save:TETRIS:v1` and
  `gamex:builtin-save:SPACE_INVADERS:v1`.
- Automated save-state coverage: 4 Vitest files, 29/29 tests passed. The tests
  cover store envelopes and failure handling, three-way routing, exact built-in
  keys, Tetris board/gameplay validation, and Space Invaders player/enemy/missile
  validation.
- Full repository build passed and synchronized `public/game-boy` through
  `scripts/build-all.mjs`; no release files were copied by hand.
- Repository regression: 11/11 `verify:*` scripts passed, including the truthful
  15 + 2 inventory, ROM/header/checksum/license/source audit, cartridge layout,
  Pixi runtime, LCD refresh, Hub frame, language bridge, and both application
  shells.
- Remaining release gate: browser acceptance must insert and play every
  cartridge, exercise controls, save, refresh, reload, continue, inspect
  screenshots, and record console errors for a final 17/17 report.

## 2026-07-25 — Browser play/save/load acceptance: 17/17

- Used the prescribed `develop-web-game` Playwright client against the built Hub
  app. Every cartridge was inserted through the real cartridge controller,
  booted, exercised with Start/D-pad/A/B input, saved, refreshed in the same
  browser storage context, reinserted, loaded, and exercised again.
- Final result: 17/17 passed title matching, boot, input response, save, load,
  and continued-play checks; 0 console errors and 0 page errors.
- All 15 WasmBoy games produced non-blank LCD frames and input-dependent frame
  hashes. Static/menu-heavy games also had tailored sequences based on their
  upstream control documentation rather than relying on animation alone.
- Tetris restored Gameplay with its board, active shape, score, lines, level,
  timing, and flags; a subsequent move/drop changed the active shape state.
- Space Invaders restored round 1 with 3 lives, score 0, 40 enemies, player and
  missile state; subsequent movement/fire changed player and missile state.
- Auditable artifacts:
  `tmp/gamex-cartridge-acceptance.json` contains every per-cartridge result and
  LCD/state fingerprints; `tmp/gamex-cartridge-acceptance.html` renders the
  final 17-row acceptance table.
- The query-gated acceptance bridge used to collect runtime state was removed
  from product code after evidence capture. The final release build contains no
  acceptance-only hook.
